/*
Ejemplo callbacks:

import { readFile } from "node:fs";

readFile("./data/turnos.json", "utf-8", (error, contenido) => {
  if (error) {
    console.error(error);
    return;
  }

  console.log(contenido);
});
*/
